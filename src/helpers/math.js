module.exports = {
  addition: (x, y) => {
    return x + y;
  },
  subtraction: (x, y) => {
    return x - y;
  },
  multiply: (x, y) => {
    return x * y;
  },
  division: (x, y) => {
    return x / y;
  },
  percentage: (x, y) => {
    return (y / x) * 100;
  },
  equals: (x, y){
    return ('?');
  }
};
